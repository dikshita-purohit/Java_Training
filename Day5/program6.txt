import java.util.*;
public class TestPrj5 {
	public static void main(String args[]) {
		Scanner sc = new Scanner(System.in);	
	
		//String rotation when user gives number of rotation
		// input S1: hello, S2: ohell, rotate: 1
		//outpt: Yes the rotation value is equal to s2
		 
		System.out.print("Enter String S1: ");
		String s1 = sc.next();
		System.out.print("Enter String S2: ");
		String s2 = sc.next();
		System.out.print("Enter rotation times (number) : ");
		int rotate = sc.nextInt();
		
        	char[] s = s1.toCharArray();
        	if(a.length != b.length) {
			System.out.print("String size is not same cant be same...");
		}else {
	        for(int i=0;i<rotate;i++) {
	
	            char last = s[s.length - 1];
	        	for (int j = s.length - 1; j > 0; j--) {
	        		s[j] = s[j - 1];
	            }
	            s[0] = last;
	        }
	        String rotated = new String(s);
	        if(rotated.equals(s2)) {
	        	System.out.print("Yes the rotation value is equal to s2");
	        }else {
	        	System.out.print("Not same");
	        }
	    }
	}
}